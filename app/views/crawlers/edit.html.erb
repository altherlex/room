<%= form_for(@crawler) do |f| %>
  <% if @crawler.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@crawler.errors.count, "error") %> prohibited this crawler from being saved:</h2>

      <ul>
      <% @crawler.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">
  <div class="col-md-10">
  <div class="field">
    <%= f.label :parametrize %><br />
    <%#= f.text_area(:configuration, cols: 130, rows: 15)%>
    <%#= text_area_tag("crawler_configuration", @crawler.indentation_parametrize, cols: 130, rows: 25)%>
    <%= f.text_area(:configuration, cols: 130, rows: 15)%>
    <!--<textarea cols="130" rows="15" id="crawler_configuration" name="crawler[configuration]">
      <%#= @crawler.parametrize %>
      <%#= JSON.pretty_generate(@crawler.parametrize) %>
      <%#= ap(@crawler.parametrize, {html:false, plain:true, index:false}) %>
    </textarea>-->
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
</div>
<% end %>
